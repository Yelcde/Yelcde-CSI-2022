base = float(input('Me dê a base do triângulo: '))
altura = float(input('Me dê a altura do triângulo: '))
area = (base*altura)/2
print(f'A área do triângulo com base {base} e altura {altura} é {area}')